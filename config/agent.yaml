# Agent Manifest — AgentRegistry entry point
# AgentRegistry reads every key below at ROOT level. Runtime-tunable parameters
# live separately in config/config.yaml.
id: "EDU-C2-045"
name: "SchoolExcursionRiskAssessmentAgent"
namespace: "agent1000"
version: "1.0.0"
enabled: true
category: "Cat 2"
generation_mode: "llm"
industry: "EDU"

# Opaque compatibility metadata; this project inherits AgentBaseGraph directly.
base_type: "AgentBaseGraph"

# AgentRegistry expects one dotted import path, not split module/class fields.
class: "src.graph.graph.Graph"

# The outer trust boundary is PreProcessNode.
required_trust_level: "VERIFIED_EXTERNAL"

# Every ctx.secrets.require() key and every optional provider integration used by
# the project is declared for compile-time provisioning checks.
# Azure OpenAI credentials are resolved from the invocation secret context.
requires:
  secrets:
    - "SCHOOL_DATA_TOKEN"
    - "QDRANT_API_KEY"
    - "AZURE_OPENAI_API_KEY"
    - "AZURE_OPENAI_ENDPOINT"
    - "AZURE_OPENAI_DEPLOYMENT"
  extras: ["openai"]

# Runtime controls such as hitl and memory_enabled belong in config/config.yaml.
